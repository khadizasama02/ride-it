import React from 'react';

const PlaceInformation = (props) => {
    const {fromPlace,wherePlace } = props.places;
    return (
        <div>
           
            </div>
    );
};

export default PlaceInformation;